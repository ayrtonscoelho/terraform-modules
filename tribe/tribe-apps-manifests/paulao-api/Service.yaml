apiVersion: v1
kind: Service
metadata:
  name: paulo-api
  namespace: paulo-api
  labels:
    app: paulo-api
spec:
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app: paulo-api
  type: ClusterIP